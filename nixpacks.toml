{
  "phases": {
    "setup": {
      "nixPkgs": ["python311"]
    },
    "install": {
      "cmds": [
        "pip install -r backend/requirements.txt"
      ]
    },
    "build": {
      "cmds": []
    }
  },
  "start": {
    "cmd": "cd backend && uvicorn main_simple:app --host 0.0.0.0 --port $PORT"
  }
}
